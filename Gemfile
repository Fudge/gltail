source 'https://rubygems.org'

# Vendored, patched-for-Ruby-3+ build of the long-abandoned chipmunk 5.3.4
# C-extension. Must be declared before `gemspec` so bundler picks the path
# source in preference to the rubygems.org copy referenced by gltail.gemspec.
gem 'chipmunk', path: 'vendor/chipmunk'

# Specify your gem's dependencies in gltail.gemspec
gemspec
